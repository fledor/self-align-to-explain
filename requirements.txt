# DPO Training with LoRA Requirements
# ====================================

# Core ML frameworks
torch>=2.1.0
transformers>=4.40.0

# TRL for DPO/GRPO training (GRPOTrainer requires >=0.15.0)
trl>=0.15.0

# PEFT for LoRA adapters
peft>=0.10.0

# Datasets
datasets>=2.18.0

# Quantization support (for QLoRA)
bitsandbytes>=0.43.0

# Accelerate for distributed training
accelerate>=0.28.0

# Additional utilities
scipy>=1.11.0
sentencepiece>=0.1.99
tqdm>=4.66.0
soxr>=0.3.0  # Required by transformers for audio utilities

# Semantic similarity for counterfactual evaluation
sentence-transformers>=2.2.0

# Evaluation metrics
python-Levenshtein>=0.21.0  # Fast edit distance computation

# Optional: For logging and monitoring
# wandb>=0.16.0
# tensorboard>=2.15.0

